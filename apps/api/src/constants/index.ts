export const ROUTES = {
  ENTRIES: 'entries',
  LISTS: 'lists',
  SPACES: 'spaces',
  USERS: 'users',
  AUTH: 'auth',
  DEFINITIONS: 'definitions',
  PRACTICE: 'practice',
};

export enum Role {
  ADMIN = 'admin',
  USER = 'user',
  PREMIUM_USER = 'premium_user',
}

export enum Locale {
  EN = 'en',
  LT = 'lt',
  CZ = 'cz',
  PL = 'pl',
  DE = 'de',
}
