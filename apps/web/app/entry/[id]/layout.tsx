import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Entry editing | atmintis',
};

const Layout = ({ children }: { children: React.ReactNode }) => {
  return children;
};

export default Layout;
