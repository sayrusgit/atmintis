{
  "name": "atmintis",
  "version": "1.0.0",
  "description": "",
  "license": "GPL-3.0",
  "scripts": {
    "dev:server": "npm run start:dev --workspace=apps/api",
    "dev:client": "npm run dev --workspace=apps/web",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "workspaces": [
    "apps*/*",
    "packages*/*"
  ]
}
